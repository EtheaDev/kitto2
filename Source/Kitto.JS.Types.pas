unit Kitto.JS.Types;

interface

type
  TJSProcedure = procedure of object;
  TCSSUnit = (cssPX, cssPerc, cssEM, cssEX, cssIN, cssCM, cssMM, cssPT, cssPC, cssNone);
  TBrowser = (brUnknown, brIE, brFirefox, brChrome, brSafari, brOpera, brKonqueror, brMobileSafari);

implementation

end.
