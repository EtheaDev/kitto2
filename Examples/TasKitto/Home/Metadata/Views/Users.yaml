Type: Data

Controller: List
  AllowClose: True

MainTable:
  Model: KITTO_USERS

  Controller:
    PagingTools: False

    PopupWindow:
      Width: 400
      Height: 320

    List:
      # A (grid) layout with this default name will be used if found.
      #Layout: <view name>_List

    Form:
      # A (grid) layout with this default name will be used if found.
      #Layout: <view name>_Form

    # One subnode for each field/value to be used to filter
    # the dataset.
    KeySet: